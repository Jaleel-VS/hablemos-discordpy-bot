# Python bytecode / caches
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ruff_cache/
.cache/

# Local virtualenv — the image builds its own at /app/.venv, and `COPY . .`
# would otherwise overwrite it with a macOS venv.
.venv/

# Secrets & VCS
.env
.env.*
.git/
.gitignore

# The activity app is a SEPARATE Lightsail service — the bot never imports it.
# Excluding it saves ~185MB from the bot image build context.
activity/

# Dev-only / non-runtime files
tests/
docs/
plans/
scripts/
deploy/
.github/
README.md
AGENTS.md
CLAUDE.md
*.log
pyrightconfig.json
pytest.ini
ruff.toml
.python-version
Dockerfile
.dockerignore
.railwayignore
